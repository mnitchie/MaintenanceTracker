{{#if default}}
	<option disabled selected value={{default}}>{{default}}</option>
{{/if}}

{{#each items as |item|}}
	<option value={{item.id}} selected={{eq item.description selected.description}}>{{item.description}}</option>
{{/each}}

